services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: personal_finance_autopilot
      POSTGRES_USER: finance
      POSTGRES_PASSWORD: finance
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

